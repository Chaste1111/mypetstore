<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csu.web.mypetstore.persistence.OrderDao">

    <!-- 插入订单 -->
    <insert id="insertOrder">
        INSERT INTO ORDERS (ORDERID, USERID, ORDERDATE, SHIPADDR1, SHIPADDR2, SHIPCITY, SHIPSTATE, SHIPZIP, SHIPCOUNTRY,
                           BILLADDR1, BILLADDR2, BILLCITY, BILLSTATE, BILLZIP, BILLCOUNTRY, TOTALPRICE, PAYMENTTYPE, STATUS)
        VALUES (#{orderId}, #{username}, #{orderDate}, #{shippingAddress1}, #{shippingAddress2}, #{shippingCity},
                #{shippingState}, #{shippingZip}, #{shippingCountry}, #{billingAddress1}, #{billingAddress2},
                #{billingCity}, #{billingState}, #{billingZip}, #{billingCountry}, #{totalPrice}, #{paymentType}, 'P')
    </insert>

    <!-- 插入订单项 -->
    <insert id="insertOrderItem">
        INSERT INTO LINEITEM (ORDERID, LINENUM, ITEMID, QUANTITY, UNITPRICE)
        VALUES
        <foreach collection="lineItems" item="item" separator=",">
            (#{orderId}, #{item.lineNumber}, #{item.itemId}, #{item.quantity}, #{item.unitPrice})
        </foreach>
    </insert>

    <!-- 根据用户名查询订单列表 -->
    <select id="getOrdersByUsername" resultType="csu.web.mypetstore.domain.Order">
        SELECT ORDERID AS orderId, USERID AS username, ORDERDATE AS orderDate,
               SHIPADDR1 AS shippingAddress1, SHIPADDR2 AS shippingAddress2,
               SHIPCITY AS shippingCity, SHIPSTATE AS shippingState, SHIPZIP AS shippingZip,
               SHIPCOUNTRY AS shippingCountry, BILLADDR1 AS billingAddress1,
               BILLADDR2 AS billingAddress2, BILLCITY AS billingCity, BILLSTATE AS billingState,
               BILLZIP AS billingZip, BILLCOUNTRY AS billingCountry, TOTALPRICE AS totalPrice,
               PAYMENTTYPE AS paymentType, STATUS AS status
        FROM ORDERS WHERE USERID = #{username} ORDER BY ORDERDATE DESC
    </select>

    <!-- 根据订单ID和用户名查询单个订单 -->
    <select id="getOrderById" resultType="csu.web.mypetstore.domain.Order">
        SELECT ORDERID AS orderId, USERID AS username, ORDERDATE AS orderDate,
               SHIPADDR1 AS shippingAddress1, SHIPADDR2 AS shippingAddress2,
               SHIPCITY AS shippingCity, SHIPSTATE AS shippingState, SHIPZIP AS shippingZip,
               SHIPCOUNTRY AS shippingCountry, BILLADDR1 AS billingAddress1,
               BILLADDR2 AS billingAddress2, BILLCITY AS billingCity, BILLSTATE AS billingState,
               BILLZIP AS billingZip, BILLCOUNTRY AS billingCountry, TOTALPRICE AS totalPrice,
               PAYMENTTYPE AS paymentType, STATUS AS status
        FROM ORDERS WHERE ORDERID = #{orderId} AND USERID = #{username}
    </select>

</mapper>