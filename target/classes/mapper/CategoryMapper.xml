<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csu.web.mypetstore.persistence.CategoryDao">

    <!-- 获取所有分类列表 -->
    <select id="getCategoryList" resultType="csu.web.mypetstore.domain.Category">
        SELECT CATEGORYID AS categoryId, NAME AS name, `DESC` AS description FROM CATEGORY
    </select>

    <!-- 根据分类ID查询分类详情 -->
    <select id="getCategory" resultType="csu.web.mypetstore.domain.Category">
        SELECT CATEGORYID AS categoryId, NAME AS name, `DESC` AS description FROM CATEGORY WHERE CATEGORYID = #{categoryId}
    </select>

</mapper>