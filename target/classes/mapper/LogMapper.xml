<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="csu.web.mypetstore.persistence.LogDao">

    <!-- 插入日志记录 -->
    <insert id="insertLog">
        INSERT INTO LOG (LOGID, USERNAME, OPERATIONTYPE, OPERATIONCONTENT, OPERATIONTIME, IPADDRESS, USERAGENT)
        VALUES (#{logId}, #{username}, #{operationType}, #{operationContent}, #{operationTime}, #{ipAddress}, #{userAgent})
    </insert>

</mapper>